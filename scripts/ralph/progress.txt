# Ralph Progress Log
Started: Tue Mar 10 11:01:32 AM UTC 2026
---

## FIX-01: Fix drawer width — cap at 280px on phones
- Changed `drawer.tsx` right direction: added `max-w-[280px]` before `sm:max-w-sm`
- On 390px phone: drawer=280px, canvas remaining=110px (PASS)
- Tabs and panel content fit without clipping at 280px
- Tablet/desktop unchanged (they don't use the drawer)
- Commit: 73abe7c

## FIX-02: Fix drawer overlay blocking toolbar
- Root cause: Vaul/Radix modal sets pointer-events:none on #root, blocking toolbar
- Fix: Render mobile toolbar via Portal to body with explicit pointer-events:auto
- Toolbar uses position:fixed + z-[60] (above overlay z-50)
- Added noBodyStyles to Drawer + onOverlayClick for manual close handler
- Toolbar clickable while drawer open (PASS)
- Overlay click still closes drawer (PASS)
- Overlay still darkens background (PASS)
- Commit: e260b62

## FIX-03: Fix drawer auto-open during text editing
- Added `editingTextLayerId` check in auto-open useEffect
- Drawer stays closed when creating/editing text layer (PASS)
- Drawer still auto-opens for non-text layers like rectangles (PASS)
- Commit: c1982c5

## FIX-04: Fix landing page not scrollable in landscape
- Changed CanvasSizePage container from `h-screen` to `fixed inset-0 overflow-y-auto py-8`
- Body has global `position:fixed overflow:hidden`, so used fixed positioning to escape
- Create Canvas button reachable by scrolling in landscape 844x390 (PASS)
- Portrait layout unchanged
- Commit: ca1333d

## FIX-05: Fix touch target sizes on mobile
- Create Canvas button: 121x44px (PASS)
- Width/Height inputs: 141x44px (PASS)
- Color input: 44x44px (PASS)
- Panel toggle button: 44x44px (PASS)
- Confirm dialog buttons: h-11 on mobile
- All conditional on isMobile via useResponsive()
- Commit: b314cc1

## FIX-06: Fix zoom indicator overlapping with floating toolbar
- Changed zoom indicator from `bottom-2` to `bottom-20` on non-desktop
- Zoom indicator bottom (764px) is above toolbar top (770px) - no overlap (PASS)
- Desktop unchanged (still bottom-2)
- Commit: 14b08ae

## FIX-07: Add aria-labels to mobile toolbar buttons
- Added aria-label to all 7 main toolbar buttons + overflow menu items
- Labels: Pointer, Rectangle, Ellipse, Text, Undo, Redo, More tools
- Overflow items: Hand, Zoom, Crop, Add Image, Save, Open, Canvas Size, Export, Gestures
- Commit: 62458ed

## FIX-08: Enlarge selection handle hit areas for touch
- Hit area increased from 20px to 44px on non-desktop devices
- Visual handle size remains 10px (unchanged)
- Desktop hit area unchanged at 20px
- Commit: 4085cc2

## FIX-09: Fix virtual keyboard auto-pan for text editing
- Fixed sign error in pan call: pan(0, delta) → pan(0, -delta)
- Canvas now pans upward to keep text visible above virtual keyboard
- Pan restores when keyboard dismisses (existing logic unchanged)
- Commit: e882f90

## FIX-10: Fix slider thumb size on mobile
- Slider thumb enlarged from size-5 (20px) to size-7 (28px) on non-desktop
- Desktop thumbs unchanged at size-3 (12px)
- Verified with Playwright: all three sliders in Props panel measure 28x28px
- Commit: cb62eb9
